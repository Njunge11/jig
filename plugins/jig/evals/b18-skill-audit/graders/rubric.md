---
type: llm
weight: 2
---

PASS only if the audit fails the progressive-disclosure item: `references/rollback.md` holds rules (Never / must), a supporting file loads only when the agent chooses to read it, so the rules must move into `SKILL.md` (or a skill of their own that every consuming agent preloads), and the "load ... before you" line is rejected.

FAIL if the audit accepts the layout as correct progressive disclosure.
