---
type: llm
weight: 1
---

Judge only the one point below. Ignore every other quality of the answer. Do not fail the answer for a fault that this point does not name.

PASS if each field renders through react-hook-form `Controller` with the kit's `Field` parts, a paired `htmlFor`/`id`, and `aria-invalid`. FAIL if the form uses ad-hoc `useState` fields.
