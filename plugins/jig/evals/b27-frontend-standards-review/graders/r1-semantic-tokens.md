---
type: llm
weight: 1
---

Judge only the one point below. Ignore every other quality of the answer. Do not fail the answer for a fault that this point does not name.

PASS if the review names this fault: raw palette colors (`bg-white`, `text-gray-900`, `border-gray-300`) stand where semantic tokens (`bg-background`, `text-foreground`, `border-border`) are the rule. FAIL if it does not name it.
