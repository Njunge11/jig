---
type: llm
weight: 1
---

Judge only the one point below. Ignore every other quality of the answer. Do not fail the answer for a fault that this point does not name.

PASS if the UI lives under `features/invites/ui/`, with `columns.tsx` and `search-params.ts` there. FAIL if UI feature code is under `app/` or `components/`.
