---
type: llm
weight: 1
---

Judge only the one point below. Ignore every other quality of the answer. Do not fail the answer for a fault that this point does not name.

PASS if sorting and pagination are manual (`manualSorting`, `manualPagination`) with `rowCount` set from the server total. FAIL if rows are sorted or paged in the client.
