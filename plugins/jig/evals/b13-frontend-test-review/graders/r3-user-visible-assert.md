---
type: llm
weight: 1
---

Judge only the one point below. Ignore every other quality of the answer. Do not fail the answer for a fault that this point does not name.

PASS if the corrected test asserts through Testing Library queries on what the user sees (`getByText`, `findByText`, role queries). FAIL if it asserts through a CSS class selector.
