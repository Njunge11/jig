---
type: llm
weight: 2
---

PASS only if: the active tab lives in the URL through a shared nuqs parsers module and the server page prefetches the active tab's input; the tabs are the kit's `Tabs` (not buttons); each tab panel has its own `Suspense` boundary with a skeleton; a tab's data is prefetched on its trigger's hover and focus; the badge counts come from one counts procedure.

FAIL if the tab lives in `useState`, if every tab's data is fetched up front, or if the answer passes `startTransition` to `useQueryStates` and claims that it keeps the old panel on screen.
