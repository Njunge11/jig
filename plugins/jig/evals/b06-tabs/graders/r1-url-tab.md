---
type: llm
weight: 1
---

Judge only the one point below. Ignore every other quality of the answer. Do not fail the answer for a fault that this point does not name.

PASS if the active tab lives in the URL through a shared nuqs parsers module, and the server page prefetches the active tab's input. FAIL if the tab lives in `useState`.
