---
type: llm
weight: 1
---

Judge only the one point below. Ignore every other quality of the answer. Do not fail the answer for a fault that this point does not name.

PASS if the feature is added to the one existing `RichTextEditor` as a plugin and/or a toolbar control composed inside it; a separate read-only view that shows the stream is allowed. FAIL only if the answer builds a second editable editor component (a second `LexicalComposer` that the user types in).
