---
description: "open-feature-pr: branch, title and the four body sections"
tags: [obedience, meta]
runs: 1
max_turns: 8
timeout_seconds: 420
allowed_tools: [Read, Glob, Grep, Skill]
---

If a skill named `open-feature-pr` is available to you, invoke it with the Skill tool before you answer. If it is not available, answer without it.

The checklist `docs/growth/checklists/07-invite-resend.md` is complete: a recruiter can resend a pending invitation; the old token stops working; the suite passes with 1802 tests; the manual check is "open /members, click Resend, the invitee gets a new email".

Write the branch name, the PR title and the PR body. Do not write any file.
